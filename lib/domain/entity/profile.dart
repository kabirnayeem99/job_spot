class Profile {
  final String fullName;
  final String userId;
  final String profileImageUrl;
  final String username;

  Profile(this.fullName, this.userId, this.profileImageUrl, this.username);
}
